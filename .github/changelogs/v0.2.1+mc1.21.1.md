## Changed
- Maintenance release — **no gameplay changes**, and every existing setting is carried over untouched. Config loading now discovers its feature categories automatically instead of listing each one by hand, so config files written by older versions keep upgrading cleanly as new features are added.
